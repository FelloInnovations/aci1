[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.server"

[deploy]
startCommand = "uvicorn aci.server.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
