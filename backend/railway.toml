[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.server"

[deploy]
startCommand = "sh -c 'bash /start.sh'"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
